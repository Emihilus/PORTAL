{% extends 'base.html.twig' %}

{% block title %}Wystaw komentarz{{parent()}}{% endblock %}

{% block body %}

<h1>Wystaw komentarz do aukcji</h1>

<div class="row mt-2">
	<ul id="auctions"
				class="list-group col">

				

	<a class="list-group-item list-group-item-dark list-group-item-action {% if date(auction[0].endsAt) < date() %}text-secondary{% endif %}"><img src="{% if auction.filename is defined and auction.filename != '' %}{{asset(tip~auction.filename)}}{% endif %}" width="75">
	<div class='d-inline-block align-middle m-1 p-1'>
		<span class='d-block'><b>{{auction.title}}</b></span>
		<span> ends
		
			{{ auction.endsAt|ago }} </span></div> HO: <b>{{auction.hghst/100}} PLN</b>

			{% if (app.user and app.user.username == auction.0.byUser.username) or (app.user and app.user.username == auction.hghstOfferOwner and date(auction[0].endsAt) < date()) %}
			<button class="btn-primary btn" onclick="task({{auction.0.id}},{% if app.user.username == auction.0.byUser.username and (app.user and app.user.username == auction.hghstOfferOwner and date(auction[0].endsAt) < date()) %}1{% elseif app.user.username == auction.0.byUser.username %}
			2{% elseif app.user and app.user.username == auction.hghstOfferOwner and date(auction[0].endsAt) < date() %}3{% endif %})">Opcje</button> {% endif %}

			<img id="fid_{{auction.0.id}}" src="{% if not app.user or auction.0.likedByUsers|length==0 %}{{asset('interface/favd.png')}}{% else %}{{asset('interface/fave.png')}}{% endif %}" onclick='toggleFavorite({{auction.0.id}})' />

				 {# {{ dump(auction) }}   #}
	</a>

	</ul>

</div>


{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
{% endblock %}