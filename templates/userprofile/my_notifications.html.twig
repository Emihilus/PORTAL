{% extends 'base.html.twig' %}

{% block title %}Powiadomienia{{parent()}}
{% endblock %}

{% block body %}

	{% for notification in notifications %}
        <p>{% if notification.wasNull %}<b>{% endif %}{{notification.createdAt|ago}}: {{notification.message}}{% if 'komenatrz' in notification.message %} <a href="{{path('profile-details', {'username': app.user})}}">Zobacz</a>{% endif %}{% if notification.wasNull %}</b>{% endif %}</p>
        
    {% endfor %}

{% endblock %}
